{
  "name": "umja-space",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "postinstall": "DATABASE_URL=${umoja_PRISMA_DATABASE_URL:-$DATABASE_URL} DIRECT_URL=${umoja_DATABASE_URL:-$DIRECT_URL} prisma generate",
    "db:generate": "DATABASE_URL=${umoja_PRISMA_DATABASE_URL:-$DATABASE_URL} DIRECT_URL=${umoja_DATABASE_URL:-$DIRECT_URL} prisma generate",
    "db:push": "DATABASE_URL=${umoja_PRISMA_DATABASE_URL:-$DATABASE_URL} DIRECT_URL=${umoja_DATABASE_URL:-$DIRECT_URL} prisma db push",
    "db:migrate": "DATABASE_URL=${umoja_PRISMA_DATABASE_URL:-$DATABASE_URL} DIRECT_URL=${umoja_DATABASE_URL:-$DIRECT_URL} prisma migrate dev",
    "db:seed": "DATABASE_URL=${umoja_PRISMA_DATABASE_URL:-$DATABASE_URL} DIRECT_URL=${umoja_DATABASE_URL:-$DIRECT_URL} prisma db seed",
    "db:migrate:deploy": "DATABASE_URL=${umoja_PRISMA_DATABASE_URL:-$DATABASE_URL} DIRECT_URL=${umoja_DATABASE_URL:-$DIRECT_URL} prisma migrate deploy",
    "db:seed:run": "DATABASE_URL=${umoja_PRISMA_DATABASE_URL:-$DATABASE_URL} DIRECT_URL=${umoja_DATABASE_URL:-$DIRECT_URL} prisma db seed"
  },
  "prisma": {
    "seed": "node prisma/seed.mjs"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "bcryptjs": "^2.4.3",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "date-fns": "^4.1.0",
    "lucide-react": "^0.577.0",
    "next": "16.1.6",
    "radix-ui": "^1.4.3",
    "react": "19.2.3",
    "react-day-picker": "^9.14.0",
    "react-dom": "19.2.3",
    "recharts": "2.15.4",
    "shadcn": "^4.0.5",
    "tailwind-merge": "^3.5.0",
    "tw-animate-css": "^1.4.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.1.6",
    "prisma": "^6.6.0",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
